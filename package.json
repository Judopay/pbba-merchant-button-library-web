{
  "name": "zapp-pbba-library",
  "version": "3.1.1",
  "description": "PbBA library node project",
  "main": "zapp.js",
  "private": true,
  "scripts": {
    "build": "node build_scripts/index.js -c configuration/pbbacustomconfig.js -i node_modules,build_scripts,configuration",
    "build:staging": "node build_scripts/index.js -c configuration/pbbacustomconfig.js -i node_modules,build_scripts,configuration"
  },
  "dependencies": {
    "commander": "latest",
    "fs-extra": "^9.0.0",
    "glob": "^7.1.6",
    "node-minify": "^3.6.0"
  }
}